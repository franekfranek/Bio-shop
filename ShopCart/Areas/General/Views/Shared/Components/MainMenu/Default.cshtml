@model IEnumerable<Page>

@foreach (var item in Model)
{
    @if (item.Slug != "home")
    {
        <a asp-area="" asp-controller="Pages" asp-action="Page" 
               asp-route-slug="@item.Slug">@item.Title</a> 
    }
}